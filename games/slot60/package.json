{
  "name": "slot60",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "replay:generate": "node ./tools/generate-replay.mjs",
    "replay:submit": "node ./tools/submit-replay.mjs"
  },
  "devDependencies": {
    "phaser": "^3.80.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.11"
  }
}
